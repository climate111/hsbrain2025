"scripts": {
  "build": "strapi build",
  "start": "strapi start",
  "console": "strapi console",
  "deploy": "strapi deploy",
  "dev": "strapi develop",
  "develop": "strapi build && strapi start",
  "strapi": "strapi",
  "upgrade": "npx @strapi/upgrade latest",
  "upgrade:dry": "npx @strapi/upgrade latest --dry",
  "postinstall": "npm install --omit=dev && npm run build"
}